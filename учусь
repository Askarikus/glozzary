use это alias для класса
единообразный синтакс если функция возвращает функцию:
f()('параметр')

https://tools.ietf.org/html/rfc6749#section-4.3 - что такое? стандарты?

javascript php кардинально различаются по областям видимсти и контекстам видимости функций

pgsql
mysql - дравера БД

строковые литералы в одинарных кавычках
что такое гидрация? извлечение из базы данных

trait - не клас и не тип, просто вставка

множественного наследования в php нет

ActiveRecord - запись сама надо собой делает операции
$user = new User();
$user->setEmail('email@email.ru');
$user->save();

рефлексия - способность кода исследовать самого себя, особенность php

activerecord - wahelp
datamapper - bonushelp


/**
 * invocationStrategy Стратегия для Slim Framework
 *
 * Не передаем в контроллеры аргументы, начинающиеся с "__",
 * считаем их служебными (для Middleware)
 */
 
 в Dockerfile мы прописываем expose 9000 для того, чтобы nginx связывался с php-fpm
 затем через настройку fastcgi_pass php-fpm:9000; задается путь от nginx к php-fpm
 и далее root /var/www/realworld/public;
