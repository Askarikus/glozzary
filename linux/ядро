лежит в директории /boot и называется vmlinuz

uname -r
версию ядра можно увидеть в с помощью команды uname -r
du -h /boot/vmlinuz*
8,6M	/boot/vmlinuz-5.14.0-1054-oem

ls /lib/modules/$(uname -r) - модули текущего ядра

lscpu, lspci, lshw

dmesg - описание в реальном времени работы ядра(загрузки модулей)

lsmod - список загруженных модулей

nproc - количество ядер
ulimit - количество открытых файлов одновременно
