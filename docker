sudo groupadd docker - создать группу докера

sudo usermod -aG docker $USER - добавить в группу текущего юзера

newgrp docker - применяет новые настройки групп пользователя

docker-compose up --build --remove-orphans создает новые контейнер таким образом, что
последующий запуск через make up уже будет запускать свежие
